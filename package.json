{
  "name": "pokato",
  "version": "0.0.0",
  "description": "A pseudo-lens library for TypeScript",
  "author": "Łukasz Hanuszczak <lukasz.hanuszczak@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "$($(which yarn || which npm) bin)/tsc",
    "watch": "$($(which yarn || which npm) bin)/tsc --watch"
  },
  "devDependencies": {
    "typescript": "^2.1.4"
  }
}
